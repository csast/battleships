// name must be unique
// add gold cap

export interface Player {
    id: number;
    name: string; //must be unique
    gold: number;
    atk: number;
    hp: number; 
    lck: number; 
    totalGoldStolen: number; 
  }

